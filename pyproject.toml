[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "fuel-depot"
version = "0.1.0"
description = "Fuel depot management system with dispatch tracking"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
